streamlit>=1.32.0
yfinance>=0.2.37
pandas>=2.0.0
numpy>=1.26.0
anthropic>=0.25.0

# Surveillance layer — all free, no API keys required
# vaderSentiment: local sentiment scoring of news headlines (no API needed)
# If not installed, the screener falls back to a built-in keyword scorer
vaderSentiment>=3.3.2
